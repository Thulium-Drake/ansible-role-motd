---
# Name of the script to deploy
#
# You can set this to any script template that can be resolved by Ansible
motd_script: 'update_motd.sh.j2'

# For systems that need a CIS compliant authorization warning
motd_authorization_warning: false

# The banner set for issue and issue.net, default is empty
issue_banner: ''
